#include <stdio.h>
/**
 * main- prints 'with proper grammar, but the outcome is a piece of art, ' using the printf function
 * return valeu will be return (0);
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
